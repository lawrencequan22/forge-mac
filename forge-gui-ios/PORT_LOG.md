# Forge iOS — Port Progress Log

Durable, append-only record of every build/test iteration. The **last entry under "Run log" is the
current state.** Structured run entries are generated by `scripts/ios-run.sh` (raw artifacts —
forge.log, crash reports — land under `build/ios-logs/<stamp>/`, which is git-ignored; only this
summary is committed). See `IOS_PORT.md` for the full engineering handoff.

**Recommended loop:** `./scripts/build-ios.sh device`  →  `./scripts/ios-run.sh "what I changed"`
→ read the new entry below → fix the next blocker → repeat.

---

## Milestones (solved blockers, in order)

| # | blocker | fix | result |
|---|---------|-----|--------|
| 1 | Java records unreadable by MobiVM | ASM de-record transform (`tools/RecordDesugar.java`) | ✓ 15.6k classes AOT-compiled |
| 2 | Jetty bytecode breaks AOT | exclude `org.eclipse.jetty:*` (pom) | ✓ |
| 3 | Sim can't link (device-only natives) | target real devices | ✓ |
| 4 | Read-only assets / unwritable data | bundle path + Documents container (`Main.java`, `ForgeProfileProperties`) | ✓ |
| 5 | Letterboxed UI | `Info.plist` launch screen + full-screen | ✓ full-res on iPad/iPhone |
| 6 | Launch watchdog deadlock (`0x8BADF00D`) | `ExceptionHandler` FileLock → plain log on iOS | ✓ past scene-create |
| 7 | tinylog `NoClassDefFound ManagementFactory` | boot-classpath stub | ✓ |
| 8 | `UnsatisfiedLinkError` GL (stale `.a`) | libGDX 1.13.5 `natives-ios` xcframeworks | ✓ GL works (textures) |
| 9 | `java.nio.file` absent | File-backed shim + `File.toPath()` rt-patch | ✓ |
| 10 | **EnumMap `AssertionError`** (current) | **SPIKE: diagnosed** — MobiVM bug, reflective `values()` fails on *large* enums (FPref=285, TrackableProperty=216 fail; ≤89 OK). NOT a general reflection wall. Fix = non-reflective enum-constants registry + rt-patch of `Enum.getSharedConstants` (see IOS_PORT.md §7). | root cause found; fix pending |

**Current state:** builds → signs → installs → launches on iPad; runs through native init, logging,
GL, and the runtime-API shims into `Forge.create()`; crashes loading the 285-constant `FPref` enum
on the large-enum reflection bug. Existential "can RoboVM do Forge's reflection?" risk is **retired**
(reflection works; only oversized enums fail).

---

## Run log

_(entries appended by `scripts/ios-run.sh` — newest at the bottom)_

### 20260630-155254
- commit: `2a0779dd72 ios-port-wip` — iOS spike: diagnose the EnumMap blocker — MobiVM fails reflective values() on large enums
- testing: spike end-state: large-enum reflection blocker (FPref); verifying ios-run.sh logging
- status: **CRASH**
- blocker: `EDT > java.lang.AssertionError  ||  at forge.localinstance.properties.PreferencesStore.<init>(PreferencesStore.java:43)`
- furthest log line: `at forge.ios.Main.main(Main.java:89)`
- artifacts: `build/ios-logs/20260630-155254/` (forge.log, enum*.log, crashes/ [0], launch.txt)
