<config>
  <executableName>${app.executable}</executableName>
  <mainClass>${app.mainclass}</mainClass>
  <os>ios</os>
  <arch>arm64</arch>
  <!-- forge-mac: tree-shaking is OFF (the default). Forge leans heavily on reflection (card
       scripts, XStream, EnumMap over FPref, etc.); the conservative tree-shaker strips
       reflectively-reached members and causes runtime AssertionError/NoSuchMethod. Jetty (the only
       reason we briefly enabled shaking) is already excluded via forge-gui-ios/pom.xml, so keeping
       every class is safe. Binary is larger; correctness wins. -->
  <treeShaker>none</treeShaker>
  <resources>
    <resource>
      <directory>../forge-gui</directory>
      <includes>
        <include>res/**</include>
      </includes>
    </resource>
    <resource>
      <directory>res</directory>
    </resource>
  </resources>
  <!-- forge-mac: provide java.lang.management.* (absent from MobiVM's iOS runtime) so tinylog can
       initialize. java.* must be on the boot classpath; a regular dependency is ignored for it. -->
  <bootclasspath>
    <classpathentry>libs/management-stub.jar</classpathentry>
    <classpathentry>libs/nio-file-stub.jar</classpathentry>
  </bootclasspath>
  <target>ios</target>
  <iosInfoPList>Info.plist.xml</iosInfoPList>
  <forceLinkClasses>
    <pattern>com.android.okhttp.HttpHandler</pattern>
    <pattern>com.android.okhttp.HttpsHandler</pattern>
    <pattern>com.android.org.conscrypt.**</pattern>
    <pattern>com.android.org.bouncycastle.jce.provider.BouncyCastleProvider</pattern>
    <pattern>com.android.org.bouncycastle.jcajce.provider.keystore.BC$Mappings</pattern>
    <pattern>com.android.org.bouncycastle.jcajce.provider.keystore.bc.BcKeyStoreSpi</pattern>
    <pattern>com.android.org.bouncycastle.jcajce.provider.keystore.bc.BcKeyStoreSpi$Std</pattern>
    <pattern>com.android.org.bouncycastle.jce.provider.PKIXCertPathValidatorSpi</pattern>
    <pattern>com.android.org.bouncycastle.crypto.digests.AndroidDigestFactoryOpenSSL</pattern>
    <pattern>org.apache.harmony.security.provider.cert.DRLCertFactory</pattern>
    <pattern>org.apache.harmony.security.provider.crypto.CryptoProvider</pattern>
    <pattern>org.apache.xalan.processor.TransformerFactoryImpl</pattern>
    <pattern>java.util.logging.**</pattern>
    <pattern>com.badlogic.gdx.**</pattern>
  </forceLinkClasses>
  <!-- forge-mac: native libs now come from the libGDX 1.13.5 natives-ios jars (xcframeworks,
       auto-linked by RoboVM). The old hand-committed libs/*.a were stale and ABI-mismatched. -->
  <frameworks>
    <framework>UIKit</framework>
    <framework>OpenGLES</framework>
    <framework>QuartzCore</framework>
    <framework>CoreGraphics</framework>
    <framework>OpenAL</framework>
    <framework>AudioToolbox</framework>
    <framework>AVFoundation</framework>
  </frameworks>
</config>